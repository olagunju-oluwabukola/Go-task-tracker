# Go-task-tracker

A simple, fast, and persistent command-line interface (CLI) for managing daily tasks, built with Go.

# Features
- Add new tasks with unique IDs.
- List all tasks with their completion status.
- Mark tasks as done.
- Delete tasks from the list.
- Automatic data persistence using a local JSON file.

